#include "main.h"

/**
  *_abs - Compute the absolute value of an integer
  *@n: the integer
  *Return: the absolute value of the integere
  */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (0);
}
