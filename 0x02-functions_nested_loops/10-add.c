#include "main.h"
#include <stdio.h>

/**
  *add - Entry point
  *Descrption - add two numbers
  *@a : one of the mumbers to be added
  *@b: the second number
  *Return: sum (success)
  */
int add(int a, int b)
{
	return (a + b);
}
